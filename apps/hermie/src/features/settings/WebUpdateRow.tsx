/**
 * The "a newer Hermie Web is out" row.
 *
 * There is no Hermie Web off the web: the phones and the Mac are store builds,
 * updated by the store. `WebUpdateRow.web.tsx` is the real one.
 */
export function WebUpdateRow() {
  return null
}
